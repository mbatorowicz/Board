services:
  board:
    container_name: board
    build: .
    ports:
      - "${BOARD_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - ./data:/app/.data
    restart: unless-stopped
